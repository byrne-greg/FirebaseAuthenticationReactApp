import FirebaseContext, {
  withFirebase,
  FirebaseContextProvider
} from "./context";
import Firebase from "./Firebase";

export default Firebase;

export { FirebaseContext, FirebaseContextProvider, withFirebase };
